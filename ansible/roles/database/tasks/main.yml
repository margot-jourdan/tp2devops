- name: Launch database
  docker_container:
    name: database
    image: margotj/tpdevop:database
    pull: yes
    state: started
    networks:
      - name: app-network
    env:
      POSTGRES_PASSWORD: "{{ db_password }}"
      POSTGRES_USER: "{{ db_user }}"
      POSTGRES_DB: db
