language: java
script:
  - cd ./simple-api/
  - mvn clean org.jacoco:jacoco-maven-plugin:prepare-agent install sonar:sonar -Dsonar.projectKey=projetdevops
  - cdd ..

  - docker login -u $user -p $password

  - docker build -t $user/tpdevop:database database
  - docker build -t $user/tpdevop:httpd httpd
  - docker build -t $user/tpdevop:simple-api simple-api

  - docker push $user/tpdevop:database
  - docker push $user/tpdevop:httpd
  - docker push $user/tpdevop:simple-api

cache:
  directories:
    - $HOME/.m2
service:
  - docker

addons:
  sonarcloud:
    organization: "devops-org"
    token: $sonarcloudtoken

